package nofutureball;

public enum Augmentation {
	W_TRIPLESHOT(), W_RANGE(), W_MEELE(),
	WI_EXPLOSIVES(), WI_HEALING(), WI_SHIELD(),
	A_SPIKED(), A_NANOHEALING(), A_HEAVY();
	
	Augmentation() {
		
	}
}
